require("options").setup({})    -- Set options like tab width, indent and etc
require("plugins").setup({})    -- Add mini.nvim plugins and setup them
require("lsp").setup({})        -- Configure and setup gopls and sqlc
require("keymaps").setup({})    -- Set custom shortcuts
require("autocmds").setup({})   -- Add auto commands
